/**
 * @author : AryanRanjane(ranjanearyan82@gmail.com)
 * @date   : 21/8/24
 * @brief  : Demonstration of "use strict"
 */

/**
 * Javascript code keeps changing the javascript maintainers and creators keep changing the code 
 * what happens is the browser might face compatability issues. which means the old code in js still works 
 * if we do slight changes the entire code might crash and cause bugs. to not let that happen 
 * js supports something called as 'use strict' this lets the functions to be used in modern way or latest way 
 * by default : the functions work in old way if we want them to work in modern way we use 'use strict'
 */
"use strict"'

